# ========= 基础服务配置 =========
# 单文件最大上传体积（支持 B/KB/MB/GB）
MAX_FILE_SIZE=100MB
# 允许上传的扩展名（逗号分隔，不带点）
ALLOWED_TYPES=pdf,docx,pptx,html,png,jpg,jpeg,xlsx
# 数据根目录（会在其下使用 input/output）
DATA_DIR=./data
# 是否开启 FastAPI debug 模式
DEBUG=false
# 是否自动清理历史任务目录
AUTO_CLEANUP=false
# 自动清理阈值（小时）
CLEANUP_MAX_AGE_HOURS=240

# ========= 转换参数（对齐原 CLI 常用项） =========
# 是否启用 PDF 按页 VL 转写主流程
PDF_VL_PRIMARY=true
# PDF 渲染 DPI（常用 180）
PDF_VL_DPI=180
# 页级并发 worker 数
PDF_VL_WORKERS=10
# DashScope / Qwen 模型名
LLM_MODEL=qwen3.5-35b-a3b
# 每页可疑表格二次校对数量上限
PDF_VL_TABLE_SECOND_PASS_MAX_TABLES=5
# 每个文档最大处理页数（空表示不限制）
MAX_NUM_PAGES=30
# LLM 单次返回 token 上限
LLM_MAX_TOKENS=16384
# LLM 采样温度
LLM_TEMPERATURE=0
# 是否开启 Qwen thinking（默认 true）
LLM_ENABLE_THINKING=true
# 是否生成表格信息转述
LLM_TABLE_CAPTION=true
# 表格信息转述最大字符数
LLM_TABLE_CAPTION_MAX_CHARS=500
# 是否按图题裁切局部插图
PDF_CAPTION_CROP_FIGURES=true

# ========= 第三方密钥 =========
# 百炼 DashScope API Key（必填）
DASHSCOPE_API_KEY=

# ========= 登录认证配置（SQLite + 会话） =========
# SQLite 用户库路径
AUTH_DB_PATH=./data/auth.db
# 会话签名密钥（生产环境务必改成随机长字符串）
SESSION_SECRET=replace_with_a_strong_secret
# 系统初始化时的统一初始密码（仅初始化新用户，实际入库为哈希）
INITIAL_PASSWORD=123456!
# 管理员用户名（管理员不通过 Web 操作，仅用于预留角色）
AUTH_ADMIN_USERNAME=admin
# 普通用户名单（逗号分隔）
AUTH_USERS=user1,user2,user3,user4,user5
